import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.SimppleAttendance.app',
  appName: 'Simpple_Attendance',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
